# ArgoCD Application — OpenCode 2.0 server (streamlined, single-process)
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: opencode-v2
  namespace: argocd
  labels:
    stack: ai
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  sources:
    - repoURL: https://github.com/tylertitsworth/ai-cluster.git
      targetRevision: add/opencode-web
      path: charts/opencode-v2
      helm:
        valueFiles:
          - $values/apps/ai/values/opencode-v2.yaml
    - repoURL: https://github.com/tylertitsworth/ai-cluster.git
      targetRevision: add/opencode-web
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: opencode
  syncPolicy:
    automated:
      enabled: false
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
