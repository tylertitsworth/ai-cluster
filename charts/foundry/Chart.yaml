apiVersion: v2
name: foundry
description: Foundry Virtual Tabletop (upstream foundry-vtt chart + Tailscale ingress)
type: application
version: 0.1.0
appVersion: "13"
# The upstream foundry-vtt chart (v13.345.0, which supports configSecretName) is
# vendored under charts/foundry-vtt/ from github.com/hugoprudente/charts (git main,
# unpublished to the Helm repo). Helm auto-includes it as a subchart; no Helm-repo
# dependency is declared so ArgoCD won't re-fetch the older published 12.343.0.
