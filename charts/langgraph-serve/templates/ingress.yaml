{{- if .Values.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "langgraph-serve.fullname" . }}
  labels:
    {{- include "langgraph-serve.labels" . | nindent 4 }}
    {{- with .Values.ingress.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  ingressClassName: {{ .Values.ingress.className }}
  rules:
    - host: {{ .Values.ingress.host }}
      http:
        paths:
          - backend:
              service:
                name: {{ include "langgraph-serve.fullname" . }}-serve-svc
                port:
                  number: 8000
            path: {{ .Values.serve.routePrefix }}
            pathType: Prefix
          - backend:
              service:
                name: {{ include "langgraph-serve.fullname" . }}-dashboard
                port:
                  number: 8265
            path: /
            pathType: Prefix
  tls:
    - hosts:
        - {{ .Values.ingress.host }}
{{- end }}
