app-template:
  controllers:
    fileflows:
      replicas: 1
      strategy: Recreate

      containers:
        main:
          image:
            repository: fileflows/fileflows
            tag: latest
            pullPolicy: IfNotPresent

          env:
            TZ: America/Los_Angeles

          probes:
            liveness:
              enabled: true
              custom: true
              spec:
                httpGet:
                  path: /
                  port: 19200
                initialDelaySeconds: 60
                periodSeconds: 30
            readiness:
              enabled: true
              custom: true
              spec:
                httpGet:
                  path: /
                  port: 19200
                initialDelaySeconds: 60
                periodSeconds: 10

          resources:
            requests:
              memory: 256Mi
              cpu: 100m
            limits:
              memory: 1Gi

  service:
    main:
      controller: fileflows
      type: LoadBalancer
      ports:
        http:
          port: 19200

  ingress:
    main:
      enabled: true
      className: tailscale
      hosts:
        - host: fileflows
          paths:
            - path: /
              pathType: Prefix
              service:
                identifier: main
                port: http
      annotations:
        tailscale.com/proxy-class: "control-plane"
      tls:
        - hosts:
            - fileflows

  persistence:
    config:
      enabled: true
      existingClaim: fileflows-config
      globalMounts:
        - path: /app/Data

    temp:
      enabled: true
      type: emptyDir
      globalMounts:
        - path: /temp

    media:
      enabled: true
      existingClaim: media-library
      globalMounts:
        - path: /media
          readOnly: false

